import { Flex, chakra } from '@chakra-ui/react';

export const D_Header = chakra(Flex, {
  baseStyle: {
    height: '60px',
    background: '#fff',
    boxShadow: '0 4px 20px -11px rgba(0, 0, 0, 0.25)',
  },
});
