import { Image, chakra } from '@chakra-ui/react';

export const M_LogoImage = chakra(Image, {
  baseStyle: {
    height: '30px',
  },
});

export const P_LogoImage = chakra(Image, {
  baseStyle: {
    height: '35px',
  },
});
