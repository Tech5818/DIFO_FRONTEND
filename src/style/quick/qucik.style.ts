import { Flex, chakra } from '@chakra-ui/react';

export const M_Quick = chakra(Flex, {
  baseStyle: {
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: '10px',
    padding: '30px 40px',
  },
});
